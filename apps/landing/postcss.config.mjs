// Tailwind v4's PostCSS plugin, the Next equivalent of the Vite plugin web
// uses. There is no Tailwind config file — the theme lives in @kp-app/ui.
export default {
  plugins: { '@tailwindcss/postcss': {} },
}
