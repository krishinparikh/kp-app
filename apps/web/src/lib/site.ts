/**
 * Site-wide constants. Anything that names or describes the product as a whole
 * belongs here rather than inline in components, so renaming is one edit.
 */
export const site = {
  name: 'kp-app',
  description: 'Personal finance tracking.',
} as const
