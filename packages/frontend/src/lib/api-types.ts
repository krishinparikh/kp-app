/**
 * GENERATED FILE — do not edit.
 *
 * Regenerate with `pnpm gen:api` (needs the backend up: `make up`).
 * This placeholder exists so the app typechecks before the API has any routes;
 * the first generation run overwrites it wholesale.
 */

export interface paths {}

export type webhooks = Record<string, never>

export interface components {
  schemas: never
  responses: never
  parameters: never
  requestBodies: never
  headers: never
  pathItems: never
}

export type operations = Record<string, never>
